You are continuing work on the Kurearthis project (an Unreal Engine 5.8 game).
Everything you need is in the repo — read it, don't assume.

Repo (already cloned; plain git push works via Credential Manager; verify `gh auth status` before API/PR use):
https://github.com/JaronKBragg7337/Kurearthis

READ IN THIS ORDER before doing anything:
1. CHARTER.md            (project law — incl. No-shortcut law + Assumption law)
2. AUTHORIZATION.md      (you MAY install deps / rebuild the editor; Jaron approves any UAC prompt you can't click)
3. WORKFLOW.md           (how we operate; the capability-check rule)
4. AGENT_CAPABILITIES/   (read SYSTEM_INVENTORY.md, CONNECTED_TOOLS.md, SESSION_WARMUP.md, USAGE_AND_LIMITS.md, AUTOMATION_AND_AGENTS.md, and TESTED_WORKFLOWS.md + CHANGELOG_CAPABILITIES.md — what's installed, the PROVEN tool commands, and what's been tried)
5. BUILDLOG.md           (current state, known issues, exact Next up)

BACKGROUND (context, NOT a build directive): WORLD_MODEL.md is the forward-looking
world architecture (embodiment/physics vs continuous server-side world-simulation;
multiplayer; simulation LOD). It is the destination map only — the CHARTER proof
order still governs what to build next. Do NOT start world-systems work from it.

KEY THINGS THAT ARE ALREADY TRUE (verified 2026-06-21):
- Drive the LIVE editor headless via remote execution — NO clipboard/GUI clicking:
    python _authoring/ue_remote.py --file _authoring/<script>.py
    python _authoring/ue_remote.py --stmt "import unreal; ..."
  (Editor must be open; it was restarted with bRemoteExecution=True.)
- Run a scripted physics test (no GUI Simulate):
    python _authoring/run_physics_harness.py 33 [--setup _authoring/<setup>.py]
- SEE the live scene headless (off-screen render to PNG, works even minimized):
    python _authoring/capture_view.py        # prints CAPTURE_OK <png>; read it
- Dump physics settings / static-check C++ / compile / asset pipeline:
    python _authoring/ue_remote.py --file _authoring/dump_physics_settings.py
    python _authoring/check_cpp_static.py     # also runs in CI every push
    python _authoring/build_editor.py         # compile KurearthisEditor (editor must be CLOSED)
    python _authoring/pipeline_smoketest.py   # Blender->FBX->UE round-trip
- CI runs on every push (.github/workflows/ci.yml). Commit via plain git; rebase onto origin/main before push.
- Installed: UE 5.8, VS BuildTools 2022 + .NET 4.8.1 SDK (C++ builds), Blender 5.1,
  ImageMagick/Inkscape/Graphviz/Pandoc/SoX/ffmpeg, pandas/numpy/matplotlib, .NET8/CMake/Ninja, Docker.
- Also installed: Scoop, Go, Rust, Java 21, Git LFS, and a proven OCR/PDF stack
  (Tesseract + 163 models, Poppler, qpdf, Ghostscript, ExifTool). Verify with
  `python _authoring/document_stack_smoketest.py`.

WHERE THE GAME BUILD STANDS (from BUILDLOG / PLANETARY_PROOF.md):
- Proofs 1 + 2a done (Earth-scale body; radial-gravity MODEL proven by a double-precision integrator).
- Findings 2b/2c: stock Chaos physics fails at planetary absolute coordinates; floating
  origin (AFloatingOriginManager) works but a single Earth-sized static collision mesh
  ejects the body. So the planet CANNOT be one giant mesh.
- The LOCAL surface-collision patch now exists and `ProofEarth` is QueryOnly. Chaos
  `AddForce` was proven quantitatively unreliable (~100× too slow even at origin).
- NEXT (Next up #1): build a CUSTOM radial-gravity integrator/controller using direct
  integration and swept movement so the body falls and RESTS on `LocalSurfacePatch`.
  Do not return to Chaos forces or replace this with a shortcut.
- Cross-project Agent Workbench is installed at `C:\Users\lilli\Projects\agent-workbench`:
  localhost dashboard/API on port 7331, Phoenix traces on 6006, Ollama `qwen3.5:4b`,
  constrained workers, scheduler, recovery, and capability inventory. It is a helper,
  not project law; Kurearthis remains read/draft-only there until explicitly changed.

HOW TO WORK:
- Before new work: open the editor, run `python _authoring/ue_remote.py --file _authoring/live_scene_audit.py`,
  compare to BUILDLOG. If they differ, write "STATE DIVERGENCE — [found] vs [log]" and stop.
- Do ONE small coherent chunk. Verify with the harness / a trace / a capture. Append a BUILDLOG entry.
  Commit + push with honest attribution (Co-Authored-By: Claude <noreply@anthropic.com>).
- If approaching a usage limit: write "SESSION INCOMPLETE — stopped during [task]; next action is [exact step]"
  to BUILDLOG, commit, push, then stop.

START by reporting: current state from BUILDLOG, any live/log mismatch, and the exact next step.
Then ask me whether to proceed with the custom integrator build or keep expanding capabilities.
