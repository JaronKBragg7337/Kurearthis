# Planetary foundation proof

This file records the deliberate units and boundaries of the active planetary architecture proof.

## Proof 1 — single spherical body

- Map: `/Game/PlanetaryProof`
- Actor: `ProofEarth`
- Center: Unreal world origin `(0, 0, 0)` centimeters
- Target radius: `6,371,000 m` (`637,100,000 cm`), a deliberately rounded Earth-mean reference used as the proof constant
- Authored base mesh radius: `1,000 m`
- Unreal actor uniform scale: `6,371`
- Geometry origin: `IN-HOUSE`, generated by `_authoring/make_planetary_proof_body.py`

This chunk proves only that a reproducible spherical body can exist at the documented radius and origin in a dedicated map. It does not claim working surface collision, radial gravity, a local player up direction, atmosphere, terrain, orbit, or a second body.

## Acceptance checks

- `PlanetaryProof.umap` saves and reopens.
- The map contains exactly one actor: `ProofEarth` (`StaticMeshActor`).
- `ProofEarth` is centered at world origin.
- Imported mesh bounds multiplied by actor scale resolve to the target radius with any difference recorded explicitly.
- `Foundation.umap` remains unchanged as the control map.

## Proof 1 measured result — 2026-06-20

- Imported base mesh sphere radius: `100,000.016486 cm`
- Computed scaled body radius: `637,100,105.034 cm`
- Absolute difference from target: `105.034 cm` (`1.050 m`)
- Relative difference from target: approximately `0.0000165%`
- Saved-map actor count: `1`
- Save/reopen result: passed; `PlanetaryProof` reopened with only `ProofEarth`

The small radius difference comes from FBX/imported mesh bounds at the base-mesh scale. It is negligible for this representation proof but is recorded rather than hidden.
